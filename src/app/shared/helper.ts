export class Helper {

  // public static setFile(event:any, key:any, model:any) {
  //   model[key] = event.target.files[0];
  // }
  // public static toFormData(resource:any) {
  //   let data = new FormData();
  //   for(let key of Object.keys(resource)) {
  //     if(resource[key])
  //       data.append(key, resource[key]);
  //   }
  //   return data;
  // }
}
